apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: spark-webui-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - http:
      paths:
      - path: /
        backend:
          serviceName: spark-webui-ingress
          servicePort: 8080
